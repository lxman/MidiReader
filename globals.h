#ifndef GLOBALS_H
#define GLOBALS_H

typedef enum { sequence_no, text, copy_not, inst_name, text_lyric, text_mark,
                 cue_point, chan_prefix, eot, set_tempo, smpte_offset, time_sig,
                 key_sig, seq_spec, seq_name } EventType;

#endif // GLOBALS_H

